import React, { useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  ImageBackground,
  Image,
  TouchableOpacity,
} from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import {
  widthPercentageToDP as wp,
  heightPercentageToDP as hp,
} from 'react-native-responsive-screen';

type LandingHeaderProps = {
  facilityExpanded: boolean;
  setFacilityExpanded: React.Dispatch<React.SetStateAction<boolean>>;
};

export default function LandingHeader({ facilityExpanded, setFacilityExpanded }: LandingHeaderProps) {
  return (
    <View>
      {/* Background Image with Facility and Weather Selector */}
      <View style={styles.bgImageWrapper}>
        <ImageBackground
          source={require('../assets/images-user/bg-image1.png')}
          style={styles.imageBackground}
          imageStyle={styles.imageStyle}
        >
          <View style={styles.topOverlayRow}>
            {/* Facility Info */}
            <View style={{ flexDirection: 'row', alignItems: 'center' }}>
              <TouchableOpacity
                activeOpacity={0.9}
                style={[
                  styles.facilityButton,
                  facilityExpanded && styles.facilityButtonExpanded,
                ]}
                onPress={() => setFacilityExpanded((prev) => !prev)}
              >
                {!facilityExpanded ? (
                  <View style={styles.facilityCollapsed}>
                    <Text style={styles.facilityText}>My Facility</Text>
                    <Ionicons name="chevron-down" size={12} color="#8B8B8B" style={{ marginLeft: 3 }} />
                  </View>
                ) : (
                  <View style={styles.facilityExpandedContent}>
                    <Image 
                      source={require('../assets/images-user/pin.png')}
                      style={{ width: 18, height: 18, marginRight: 6, marginTop: 2 }}
                    />
                    <View>
                      <Text style={styles.facilityTitle}>Metalware Corporation</Text>
                    </View>
                  </View>
                )}
              </TouchableOpacity>
            </View>

            {/* Weather Info */}
            <View style={styles.weatherContainer}>
              <Image
                source={require('../assets/images-user/weather.png')}
                style={styles.weatherIcon}
              />
              <Text style={styles.weatherText}>32°C°F</Text>
            </View>
          </View>
        </ImageBackground>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  // Removed: topRow, logo, searchContainer, searchIcon, input, toggleButton, toggleImage

  // Facility & Weather Row
  bgImageWrapper: {
    position: 'relative',
    width: '100%',
  },
  imageBackground: {
    height: hp('38%'),
    width: '100%',
  },
  imageStyle: {
    height: '83%',
    width: '100%',
    marginTop: hp('10%'),
  },
  topOverlayRow: {
    position: 'absolute',
    top: hp('1%'),
    left: wp('4%'),
    right: wp('4%'),
    zIndex: 10,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'flex-start',
  },
  facilityButton: {
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#ddd',
    borderRadius: 20,
    paddingVertical: 1,
    paddingHorizontal: 4,
    flexDirection: 'row',
    alignItems: 'center',
    minHeight: 10,
    minWidth: 10,
    shadowColor: '#000',
    shadowOpacity: 0.07,
    shadowRadius: 4,
    shadowOffset: { width: 0, height: 2 },
  },
  facilityCollapsed: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  facilityText: {
    fontSize: wp('2.8%'),
    color: '#8B8B8B',
    fontWeight: '400',
    paddingHorizontal: 6,
  },
  facilityButtonExpanded: {
    paddingHorizontal: 16,
    paddingVertical: 10,
    borderRadius: 8,
    minWidth: wp('10%'),
    minHeight: 10,
    alignItems: 'flex-start',
  },
  facilityExpandedContent: {
    flexDirection: 'row',
    alignItems: 'flex-start',
  },
  facilityTitle: {
    fontSize: wp('3.8%'),
    fontWeight: '400',
    color: '#222',
    marginBottom: 2,
  },
  facilitySubText: {
    fontSize: wp('3.3%'),
    color: '#888',
    fontWeight: '400',
    marginBottom: 1,
  },

  // Weather styles
  weatherContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#fff',
    paddingHorizontal: 2,
  },
  weatherIcon: {
    width: 20,
    height: 20,
    resizeMode: 'contain',
    marginRight: 6,
  },
  weatherText: {
    fontSize: wp('2.8%'),
    fontWeight: '500',
    color: '#8B8B8B',
  },
});
